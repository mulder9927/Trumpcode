# TrumpLang Syntax Reference

> *"It's a very complicated language. Very sophisticated. But I understand it better than anyone. The coders, they come up to me with tears in their eyes — big, strong coders — and they say 'Sir, I've never seen syntax this beautiful.' And they're right."*

---

## How TrumpLang Works

Every Python keyword has **multiple valid Trump phrases**. Any of them work. You pick the one that feels right. That's the freedom TrumpLang gives you — the freedom to express yourself, in your words, the way a real American would.

The phrases are **ALL CAPS**, as God and Trump intended.

Standard Python syntax for everything else — parentheses, arithmetic, string literals, variable names, colons — stays exactly the same. Because it already works perfectly. We're not changing things that don't need to be changed. That would be stupid.

---

## Functions

Define a function with any of these:

```trump
LET ME TELL YOU my_function(x, y):
    IT'S GONNA BE HUGE x + y
```

```trump
NOBODY KNOWS MORE ABOUT THIS THAN ME calculate(price, tax):
    IT'S GONNA BE HUGE price * (1 + tax)
```

```trump
I'VE BEEN SAYING THIS FOR A LONG TIME process(data):
    IT'S GONNA BE HUGE data
```

**All valid phrases for `def`:**
- `LET ME TELL YOU`
- `NOBODY KNOWS MORE ABOUT THIS THAN ME`
- `I'VE BEEN SAYING THIS FOR A LONG TIME`
- `NOBODY TALKS ABOUT THIS ENOUGH`
- `I'M THE BEST AT`
- `SO IMPORTANT, LISTEN`
- `MANY PEOPLE DON'T KNOW THIS BUT`
- `EVERYONE AGREES`
- `SO IMPORTANT`

**All valid phrases for `return`:**
- `IT'S GONNA BE HUGE`
- `TOTAL SUCCESS`
- `WE'RE DONE HERE`
- `BEAUTIFUL RESULT`
- `AND THAT'S THE WINNER`
- `EVERYBODY SAYS IT WORKS`
- `NOBODY RETURNS BETTER THAN ME`
- `WINNING`

---

## Conditionals

```trump
IF YOU LOOK AT IT score IS BIGGER THAN 90:
    LOOK, FOLKS("Tremendous score.")
BUT ALSO CONSIDER score IS BIGGER THAN 70:
    LOOK, FOLKS("Good score. Not great.")
MANY PEOPLE ARE SAYING:
    LOOK, FOLKS("Sad. Very sad.")
```

**All valid phrases for `if`:**
- `IF YOU LOOK AT IT`
- `MANY PEOPLE ARE STARTING TO SEE`
- `PEOPLE ARE STARTING TO NOTICE`
- `FRANKLY, IF`
- `I'LL TELL YOU WHAT, IF`
- `SOME PEOPLE ARE SAYING`
- `SOME PEOPLE WON'T SAY IT`
- `LOOK, THE TRUTH IS`
- `THIS I CAN TELL YOU`
- `IT'S OBVIOUS`

**All valid phrases for `elif`:**
- `BUT ALSO CONSIDER`
- `AND ANOTHER THING`
- `OR WHAT ABOUT`
- `SOME ARE ALSO SAYING`
- `ANOTHER THING`

**All valid phrases for `else`:**
- `MANY PEOPLE ARE SAYING`
- `FAKE NEWS WOULD SAY`
- `NOBODY KNOWS FOR SURE`
- `ALTERNATIVE FACT`
- `ON THE OTHER HAND`
- `BUT SOME LOSERS THINK`
- `COULD BE WRONG, BUT`
- `ALTERNATIVELY`
- `WE'LL SEE`

---

## Loops

### While Loop

```trump
counter = 0
WE'RE GONNA KEEP GOING counter IS SMALLER THAN 5:
    LOOK, FOLKS("Still going:", counter)
    counter = counter + 1
```

**All valid phrases for `while`:**
- `WE'RE GONNA KEEP GOING`
- `NEVER GIVE UP`
- `KEEP IT GOING`
- `CAN'T STOP WON'T STOP`
- `WE'RE IN THIS TOGETHER`
- `KEEP AMERICA GREAT`
- `AGAIN AND AGAIN`

### For Loop

```trump
FOR EACH AND EVERY item IN my_list:
    LOOK, FOLKS(item)
```

```trump
LOOKING AT EVERY SINGLE i IN range(10):
    LOOK, FOLKS(i)
```

**All valid phrases for `for`:**
- `FOR EACH AND EVERY`
- `LOOKING AT EVERY SINGLE`
- `GOING THROUGH EVERY`
- `WE'RE GOING THROUGH ALL OF`
- `ONE BY ONE WE LOOK AT`

### Loop Control

**Break (`break`):**
- `STOP THE COUNT`
- `SHUT IT DOWN`
- `I NEVER SAID THAT`
- `TOTAL SHUTDOWN`
- `WE'RE DONE HERE, STOP`
- `ENOUGH`

**Continue (`continue`):**
- `KEEP GOING`
- `MOVE ON`
- `SKIP THE LOSERS`
- `MOVING FORWARD`
- `NEXT ONE`

---

## Classes

```trump
MAKE AMERICA GREAT AGAIN Animal:
    LET ME TELL YOU __init__(self, name):
        self.name = name

    LET ME TELL YOU speak(self):
        LOOK, FOLKS(self.name, "says something. Probably the best thing.")
        IT'S GONNA BE HUGE self.name
```

**All valid phrases for `class`:**
- `MAKE AMERICA GREAT AGAIN`
- `THE TRUMP ORGANIZATION`
- `A TREMENDOUS ORGANIZATION CALLED`
- `WORLD CLASS`
- `BEST IN CLASS`

---

## Imports

```trump
BRING IN THE BEST os
BRING IN THE BEST sys
GET ME json
```

```trump
FROM THE GREAT os KNOWN AS operating_system
```

**All valid phrases for `import`:**
- `BRING IN THE BEST`
- `GET ME`
- `WE NEED`
- `WE'RE GETTING`
- `I KNOW PEOPLE, GET ME`

**All valid phrases for `from`:**
- `FROM THE GREAT`
- `DIRECTLY FROM`
- `COMING FROM`
- `SOURCED FROM`
- `TAKEN FROM`

**All valid phrases for `as`:**
- `KNOWN AS`
- `WE CALL IT`
- `ALSO CALLED`
- `NICKNAMED`
- `WHICH WE CALL`

---

## Exception Handling

```trump
VERY LEGAL VERY COOL:
    result = 10 / 0
IF SOMETHING GOES WRONG ZeroDivisionError:
    LOOK, FOLKS("Can't divide by zero. Even I can't do that.")
AND THAT'S FINAL:
    LOOK, FOLKS("Always runs. Always. No matter what.")
```

**All valid phrases for `try`:**
- `VERY LEGAL VERY COOL`
- `WATCH THIS`
- `WE'LL SEE WHAT HAPPENS`
- `LET'S GIVE IT A SHOT`
- `TREMENDOUS ATTEMPT`

**All valid phrases for `except`:**
- `COVFEFE`
- `IF SOMETHING GOES WRONG`
- `IN CASE OF DISASTER`
- `WHEN THINGS GO SOUTH`
- `IF THE DEEP STATE INTERFERES`

**All valid phrases for `finally`:**
- `AND THAT'S FINAL`
- `MARK MY WORDS`
- `NO MATTER WHAT`
- `BELIEVE ME IT ALWAYS HAPPENS`
- `AT THE END OF THE DAY`

**All valid phrases for `raise`:**
- `YOU'RE FIRED`
- `TOTAL DISGRACE`
- `THIS IS AN OUTRAGE`
- `UNACCEPTABLE`
- `GET OUT`

---

## Boolean Values and None

```trump
is_winning = ABSOLUTELY
is_rigged = FAKE NEWS
result = NOTHING THERE
```

**True:**
- `ABSOLUTELY`
- `ONE HUNDRED PERCENT`
- `WITHOUT QUESTION`
- `TOTALLY TRUE`
- `PERFECT`

**False:**
- `FAKE NEWS`
- `RIGGED`
- `COMPLETELY WRONG`
- `TOTALLY FALSE`
- `WITCH HUNT`

**None:**
- `NOTHING THERE`
- `EMPTY LIKE BIDEN`
- `VOID`
- `ZERO SUBSTANCE`
- `DOESN'T EXIST`

---

## Comparison Operators

You can use standard Python symbols (`>`, `<`, `==`, `!=`, `>=`, `<=`) **or** Trump phrases. Both work.

| Symbol | Trump Phrase Options |
|--------|---------------------|
| `>` | `IS BIGGER THAN` / `BEATS` / `DOMINATES` / `IS GREATER THAN` / `CRUSHES` |
| `<` | `IS SMALLER THAN` / `LOSES TO` / `IS WEAKER THAN` / `IS LESS THAN` / `IS BEHIND` |
| `==` | `IS THE SAME AS` / `MATCHES` / `IS IDENTICAL TO` / `EQUALS` |
| `!=` | `IS FAKE COMPARED TO` / `DOESN'T MATCH` / `IS NOT THE SAME AS` / `IS TOTALLY DIFFERENT FROM` |
| `>=` | `IS AT LEAST AS GREAT AS` / `IS BIGGER THAN OR MATCHES` / `IS GREATER THAN OR EQUAL TO` |
| `<=` | `IS AT MOST` / `IS SMALLER THAN OR MATCHES` / `IS LESS THAN OR EQUAL TO` |

---

## Logical Operators

**`and`:**
- `AND ALSO`
- `COMBINED WITH`
- `TOGETHER WITH`
- `PLUS ALSO`

**`or`:**
- `OR MAYBE`
- `POSSIBLY`
- `COULD ALSO BE`

**`not`:**
- `WRONG`
- `FAKE`
- `NOT TRUE`
- `TOTALLY FALSE`
- `WITCH HUNT`

---

## Print / Output

```trump
LOOK, FOLKS("Hello, America!")
SADLY("The numbers don't lie.")
YOU WON'T BELIEVE THIS("We won. By a lot.")
```

All print phrases:
- `LOOK, FOLKS`
- `SADLY`
- `YOU WON'T BELIEVE THIS`
- `FAKE NEWS SAYS`
- `I'M TELLING YOU`
- `EVERYBODY NEEDS TO KNOW`
- `I SAID IT FIRST`

---

## Input

```trump
name = ASK THE PEOPLE("What's your name? ")
LOOK, FOLKS("Hello,", name)
```

All input phrases:
- `ASK THE PEOPLE`
- `TAKE A POLL`
- `WHAT DO YOU SAY`
- `THE AUDIENCE SAYS`
- `WHAT'S YOUR ANSWER`

---

## Common Builtins

| Python | TrumpLang |
|--------|-----------|
| `len(x)` | `COUNT THE CROWD(x)` / `HOW MANY(x)` / `THE SIZE OF(x)` |
| `range(n)` | `THE NUMBERS FROM(n)` / `COUNT FROM(n)` / `ALL THE WAY FROM(n)` |
| `str(x)` | `IN WORDS(x)` / `SAY IT LOUD(x)` / `AS TEXT(x)` |
| `int(x)` | `THE NUMBER(x)` / `AS A NUMBER(x)` / `WHOLE NUMBER(x)` |
| `float(x)` | `DECIMAL NUMBER(x)` / `PRECISE NUMBER(x)` |
| `list(x)` | `THE BEST LIST(x)` / `TREMENDOUS ARRAY(x)` |
| `dict(x)` | `THE SECRET FILES(x)` / `THE DOSSIER(x)` |
| `max(x)` | `THE WINNER(x)` / `GREATEST OF(x)` / `BIGGEST OF(x)` |
| `min(x)` | `THE LOSER(x)` / `WEAKEST OF(x)` / `THE WORST OF(x)` |
| `sorted(x)` | `PUT IN ORDER(x)` / `RANKED(x)` / `ORGANIZED BEAUTIFULLY(x)` |
| `sum(x)` | `ADD THEM ALL UP(x)` / `THE GRAND TOTAL(x)` / `ALL COMBINED(x)` |
| `open(f)` | `OPEN THE BEAUTIFUL FILE(f)` / `UNLOCK THE FILE(f)` / `GET THE FILE(f)` |

---

## Context Managers (with)

```trump
IN CAHOOTS WITH open("data.txt") KNOWN AS f:
    content = f.read()
    LOOK, FOLKS(content)
```

**`with` phrases:**
- `IN CAHOOTS WITH`
- `WORKING TOGETHER WITH`
- `SIDE BY SIDE WITH`
- `TREMENDOUS PARTNERSHIP WITH`
- `ALONGSIDE`

---

## Generators (yield)

```trump
LET ME TELL YOU count_up(n):
    FOR EACH AND EVERY i IN range(n):
        HERE'S ONE FOR YOU i
```

**`yield` phrases:**
- `HERE'S ONE FOR YOU`
- `TAKE THIS ONE`
- `GIVING YOU`
- `HERE IT COMES`
- `RELEASING`

---

## Async / Await

```trump
MEANWHILE LET ME TELL YOU fetch_data(url):
    result = HOLD ON some_async_operation(url)
    IT'S GONNA BE HUGE result
```

**`async` phrases:** `MEANWHILE` / `AT THE SAME TIME` / `SIMULTANEOUSLY` / `BIGLY CONCURRENT`

**`await` phrases:** `HOLD ON` / `WAIT FOR IT` / `STAND BY` / `WE'RE WAITING ON`

---

## The Kill Switch

One phrase exists that ends everything immediately, with extreme prejudice:

```trump
EPSTEIN LIST
```

This calls `raise SystemExit(1138)`. **Do not use this casually.** It ends the program. Completely.

---

## The Rant Feature

TrumpLang has opinions. Every **third `print` call** in a `.trump` program automatically appends a Trump commentary line. You don't ask for it. You don't configure it. It just happens — like a press conference.

```
Hello, America!
The numbers are incredible.
We are winning bigly.
  >> A lot of people don't get it. Smart people do.
```

The rant rotates through a pool of Trump phrases. If the output contains trigger words (`CNN`, `HILLARY`, `FAKE`, `RIGGED`, `DOMINION`, etc.) a *negative rant* fires instead:

```
CNN is terrible.
  >> Even the fake news had to admit it was great.
```

This is not a bug. This is the feature.

---

## BIGLY — The Block Opener

In standard Python every block header ends with a colon (`:`). In TrumpLang you can use `BIGLY` instead. It's optional — a regular colon still works — but `BIGLY` is the way winners do it.

```trump
LET ME TELL YOU greet(name) BIGLY
    LOOK, FOLKS("Hello,", name)

KEEP IT GOING counter IS SMALLER THAN 10 BIGLY
    counter = counter + 1

MAKE AMERICA GREAT AGAIN Dog BIGLY
    LET ME TELL YOU __init__(self, name) BIGLY
        self.name = name
```

> `BIGLY` maps to `:` — it goes wherever a colon would go. After a function definition, class definition, if/elif/else/while/for header, try/except/finally, or with statement. Anywhere Python expects a colon, BIGLY works.

---

## Comments

Standard Python comments work fine:

```trump
# This is a comment. Very important. People say my comments are the best.
LOOK, FOLKS("Running.")  # Inline comment, also tremendous.
```

---

## A Complete Example

```trump
# TrumpLang: Greatest Hits
# A demonstration of the language. Beautiful. Historic.

BRING IN THE BEST random

LET ME TELL YOU pick_winner(candidates):
    IT'S GONNA BE HUGE random.choice(candidates)

candidates = ["Trump", "America", "Freedom", "The Economy"]

LOOKING AT EVERY SINGLE i IN THE NUMBERS FROM(1, 4):
    winner = pick_winner(candidates)
    LOOK, FOLKS("Round", i, "winner:", winner)

score = COUNT THE CROWD(candidates)

IF YOU LOOK AT IT score IS BIGGER THAN 3:
    LOOK, FOLKS("Tremendous field. The best field anyone has ever seen.")
MANY PEOPLE ARE SAYING:
    LOOK, FOLKS("Small field. Low energy.")

VERY LEGAL VERY COOL:
    result = 100 / score
    LOOK, FOLKS("Average:", result)
IF SOMETHING GOES WRONG ZeroDivisionError:
    LOOK, FOLKS("Zero candidates? Rigged.")
AND THAT'S FINAL:
    LOOK, FOLKS("Done. We won.")
```

---

*For installation instructions, see [INSTALL.md](INSTALL.md).*

*For the full phrase table embedded in the interpreter, see [`Python/trump_preprocess.c`](../Python/trump_preprocess.c).*
